import React, { Component } from "react";

import '../styles/components/Selector.scss';

class Selector extends Component {
    render() {
        return (
            <div class="selector" id={this.props.id}>
                
            </div>
        );
    }
}

export default Selector;
